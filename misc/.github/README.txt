= Contents =

[ "perlmagic.bat"
	Hints and recommended practice for using Perl under Windows. ]

[ "perlp.old"
	The old implementation in Unix Shell that was rendered obsolete, due to the discovery of a more efficient interface ("$^I") for detecting "-i".
	Mostly as a reference. ]
